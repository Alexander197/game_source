#version 460
in vec2 texCoords;
in vec3 normals;

out vec4 frag_color;
//out mat3 TBN;


uniform sampler2D tex;

void main() {
	//frag_color = texture(tex, texCoords);
	//TBN = mat3(vec3(1.0, 1.0, 1.0), vec3(1.0, 1.0, 1.0), normals);
	frag_color = vec4 (0.8, 0.8, 0.8, 1.0);
	//if(frag_color.rgb == vec3(0.0))
	//{
	//	discard;
	//}
}