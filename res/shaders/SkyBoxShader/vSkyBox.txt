#version 460
layout(location = 0) in vec3 vertex_position;
out vec3 texCoords;

uniform mat4 viewMat;
uniform mat4 projectionMat;

void main() {
	texCoords = vertex_position;
	vec4 pos = projectionMat * viewMat *  vec4(vertex_position, 1.0);
	gl_Position = pos.xyww;
}