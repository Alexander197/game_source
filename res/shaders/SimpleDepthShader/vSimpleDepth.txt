#version 460
layout(location = 0) in vec3 vertex_position;

uniform mat4 lightSpaceMatrix;
uniform mat4 modelMat;

void main() {
  
   gl_Position = lightSpaceMatrix * modelMat * vec4(vertex_position, 1.0);
}